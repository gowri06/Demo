#include <stdio.h>
int main(void) {
	int num,i=0,k,t;
	scanf("%d%d",&k,&num);

	if(num<0)
	printf("0");
	else
	{
		t=num;
	
	while(num)
	{
		t*=num%10;
		num/=10;
	}
	
	if(t==k)
	{
		printf("seed");
	 }
	 else
	   printf("0");
	}
	// your code goes here
	return 0;
}
